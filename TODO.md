# TODO

- [x] Pin Chart.js to a specific version (4.5.1) — shipped as static file
- [x] Add tests for helper functions and views with mocked Celery/Redis — 45 tests
- [ ] Update NetID to use `pip install django-saladbar` instead of in-tree app
- [x] Add `py.typed` marker for type hint support
- [ ] Rate limit the dashboard view to prevent broker load from aggressive refreshes
- [ ] Configurable permission decorators for projects using custom auth
